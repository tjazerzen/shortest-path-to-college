from model import Model, Graf
import json
from model import vse_tocke

dict_tock = {}
'''
for tocka in set(vse_tocke):
    if tocka.ime not in dict_tock.keys():
        dict_tock[tocka.ime] = tocka

slovar = {"vse_tocke": [tocka.v_slovar() for tocka in dict_tock.values()]}


with open("frekvenca_obiskov.json", "w") as dat:
    json.dump(slovar, dat, ensure_ascii=False, indent=4)
'''
